node_modules
dist
.DS_Store
*.local
ORGANIZER-API.rtf
.env

# Real personal data export (~190MB) — never belongs in git
organizer-backup.json

# Rotating auto-backups (real personal data, ~12MB each) — never belongs in git
Backup-folder/

# TypeScript incremental build cache — machine-specific, auto-regenerated
*.tsbuildinfo

# Claude Code local-only settings (tool permission grants for this machine)
.claude/settings.local.json
